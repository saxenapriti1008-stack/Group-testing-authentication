PORT=4000
CLIENT_ORIGIN=http://localhost:5173

# Option A: inline JSON (e.g. CI secret)
# FIREBASE_SERVICE_ACCOUNT_JSON=

# Option B: path to service account file (relative paths resolve from backend/ cwd)
GOOGLE_APPLICATION_CREDENTIALS=../../server/firebase/firebase-service-account.local.json
